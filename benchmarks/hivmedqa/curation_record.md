- Sources: pin the author archive and the historical native-score CSV, alongside the original Zenodo release. Historical data are downloaded individually; an unrelated old notebook containing a credential-like value is excluded.
- Transform: join recorded answers, judge prompts and judge replies; melt the five dimensions under their distinct grading protocols. Mark GPT-4o reference-paraphrasing controls separately, retaining their supplied references. Preserve native trials and full traces, and link the original release as aliases without duplicate observations.
- Review: verify source associations, original grades and the Gemini recovery; restore native iteration numbers that legacy tables reassigned. Preserve valid zeros, flag unavailable/nonnumeric grading as null, and retain ungraded attempts. Retrieval references are available for RAG runs; full retrieved passages are not.
