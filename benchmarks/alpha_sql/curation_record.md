- Import every released final SQL prediction using a pinned Alpha-SQL revision and the original BIRD development archive. Preserve full queries, questions, schemas, 11 databases and their column descriptions; intermediate search histories were not released.
- Derive grades by comparing complete SQL result sets in read-only mode. Keep the previous curation's 60-second allowance per query and retain failed reference executions as ungraded attempts. This protocol differs from BIRD's default combined timeout and does not claim the paper's aggregate accuracy.
- Use four readable stages: join predictions to questions, attach database inputs, derive execution grades, and project the shared tables. The shared source audit checks every native record, grade and attachment independently.
