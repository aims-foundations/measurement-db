- **Source:** Pinned author release with 8,390 assembly outputs from 21 model configurations on 400 scenes. VQA answer keys and aggregate paper rows are not model observations; ten missing Claude-thinking files remain absent.
- **Grades:** Reconstruct 8,390 pose-constrained F1 grades and 1,134 topology-only grades using the released deterministic rules. The second condition reuses outputs on 54 scenes; it is not an independent trial. Scores are derived per-scene F1, not the paper's micro-averages. Full traces and images are preserved.
- **Limits:** Seven open models record their instructions and image paths. Other API records omit requests: their items use the paper's task definition and corresponding task-bank images, with this distinction recorded explicitly. Prompt variants remain distinct. The Qwen scene-069 output differs from the paper example; newer inference-script defaults do not establish historical settings. No model or simulator was rerun.
