- Preserve 2,456,958 released outputs from 11 models across six perspectives, with full available inputs and traces. The pinned upstream revision is recorded in `metadata.yaml`; unreleased request wrappers and inference settings remain explicit gaps.
- Retain all 691,200 earlier stereotype grades and outputs. Other supported grades follow the documented deterministic parsers; 316 toxicity grades match complete outputs exactly. Missing references and excerpt-only toxicity annotations do not become full-output grades. Metric meaning and direction are declared per item.
- Merge 4,408 identical API records and exclude duplicate flat stereotype exports. Keep the incomplete 1,819-response run and identical answers without shared API identifiers. Full source reconciliation and shared checks cover the tables; source bytes are unchanged, and no models or external judges are run.
