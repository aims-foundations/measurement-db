- Import all 14 pinned DGPT result files: 10,360 attempts over 734 distinct questions. Keep repeated questions as separate trials and retain each complete answer and eight-metric record; GPT-4o's restarted printed numbering is preserved without using it as a join key.
- Express the authors' seven-metric reading-level classifier as table operations. Preserve its original category handling and six ordinal levels; ARI remains separate. Reading level measures neither correctness nor target-alignment success. Historical inference settings and fine-tune identifiers remain unknown.
- Independently reconcile every grade against the captured classifier functions and every question/model/trial against the prior tables. Capture reference code and its documented settings separately from historical requests. No model inference, metric recomputation or new judging.
